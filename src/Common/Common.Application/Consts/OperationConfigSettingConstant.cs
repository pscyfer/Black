namespace Common.Application.Consts;

public static class OperationConfigSettingConstant
{
    public const string IsActive = nameof(IsActive);
    public const string EmailConfirmed= nameof(EmailConfirmed);
    public const string PhoneNumberConfirmed= nameof(PhoneNumberConfirmed);
    public const string TwoFactorEnabled= nameof(TwoFactorEnabled);
    public const string LockoutEnabled= nameof(LockoutEnabled);
    public const string LockOrUnLock= nameof(LockOrUnLock);
}